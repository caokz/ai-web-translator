# AI Web Translator - 快速参考指南

## 快速开始（5分钟）

### 1. 构建扩展
```bash
cd ai-web-translator
npm run build
```

### 2. 重新加载扩展
1. 打开 `chrome://extensions/`
2. 找到 "AI Web Translator"
3. 点击刷新按钮（或禁用后启用）

### 3. 配置 API Key
1. 点击扩展图标
2. 点击"设置" → "选择模型"
3. 选择模型（如 DeepSeek）
4. 输入 API Key
5. 点击"验证API Key"
6. 点击"保存配置"

### 4. 测试翻译
1. 打开网页：https://nightlies.apache.org/flink/flink-docs-release-2.2/docs/deployment/config/
2. 点击扩展图标
3. 点击"翻译当前页面"
4. 打开 Console（F12）查看日志

---

## Console 日志速查表

### ✅ 成功流程的日志
```
🚀 Content Script initializing...
✅ Content Script initialized
🔄 Initializing translation...
✅ DOM observer started
📋 Collected 342 elements for translation
📤 Translating: "Configuration..."
📥 Translation response: { success: true, hasData: true }
✅ Translation success: "配置..."
```

### ❌ 常见错误及含义

| 日志 | 含义 | 解决方案 |
|------|------|---------|
| `🔍 hasApiKey: false` | API Key 未配置 | 在设置中配置 API Key |
| `❌ API Key未配置` | 选定的模型未配置 Key | 选择已配置的模型或添加新模型 |
| `❌ 401 Unauthorized` | API Key 无效 | 检查 Key 是否过期或有误 |
| `❌ Network error` | 网络连接问题 | 检查网络、代理、VPN |
| `Could not establish connection` | 扩展未正确加载 | 重新加载扩展 |
| `No visible log` | 扩展未初始化 | 刷新网页，检查 Console 权限 |

---

## 快速诊断（问题排查）

### 问题1：翻译不工作，但没有错误日志

**步骤**：
1. F12 打开 Console
2. 刷新网页
3. 点击扩展图标，启用翻译
4. 查看 Console 是否出现日志

**如果没有日志**：
- 检查是否选中了正确的 Console（应该是页面的 Console，不是扩展的）
- 刷新页面重新加载 Content Script
- 检查浏览器是否在隐私模式（扩展在隐私模式可能不生效）

### 问题2：看到 "hasApiKey: false" 错误

**解决**：
1. 点击扩展图标 → 设置
2. 在 "选择模型" 中选择一个模型
3. 确保 API Key 已填写
4. 点击"验证API Key"（应显示"✓ API Key 验证成功"）
5. 点击"保存配置"

### 问题3：看到 "401 Unauthorized" 错误

**解决**：
1. 检查 API Key 是否正确（注意前后空格）
2. 访问 API 提供商网站确认 Key 是否过期
3. 尝试重新生成 API Key
4. 更新扩展设置中的 Key
5. 重新测试翻译

### 问题4：Network error 或网络超时

**解决**：
1. 检查网络连接是否正常
2. 尝试访问 API 提供商网站（如 https://api.deepseek.com）
3. 如果使用了 VPN，尝试关闭
4. 检查防火墙/代理设置
5. 等待几秒后重试

---

## 日志位置

### Content Script 日志（页面翻译）
```
F12 → Console
```
看到的日志：
- 📤 Translating: ...
- 📥 Translation response: ...
- ✅ Translation success: ...

### Background Service Worker 日志（消息处理）
```
chrome://extensions/
→ 找到 "AI Web Translator"
→ 点击"Service Worker"下的链接
```
看到的日志：
- 📨 Message received: ...
- 🔍 TRANSLATE_TEXT handler: ...
- ✅ Translation completed: ...

### Translation Service 日志（API 调用）
```
同样在 Service Worker 日志中查看，包含：
- 🔍 TranslatorService.translate(): ...
- 💾 Cache hit/Cache miss
- 🔍 Model config - hasApiKey: ...
- 📤 Calling LLM API...
```

---

## 翻译流程检查清单

✅ 启用翻译前：
- [ ] API Key 已配置
- [ ] 网络连接正常
- [ ] 扩展已加载（点击图标有反应）

✅ 启用翻译时：
- [ ] Console 显示 "🚀 Content Script initializing..."
- [ ] Console 显示 "✅ Content Script initialized"
- [ ] Console 显示 "📋 Collected XXX elements"

✅ 翻译过程中：
- [ ] Console 显示 "📤 Translating: ..." 日志
- [ ] Console 显示 "📥 Translation response: ..." 日志
- [ ] 页面上逐步显示翻译内容

✅ 翻译完成后：
- [ ] Console 显示 "✅ All translations completed"
- [ ] 所有英文文本都有对应的中文翻译
- [ ] 格式和排版正确

---

## 性能基准

| 指标 | 值 |
|------|-----|
| 初始化时间 | < 1 秒 |
| 页面翻译时间（第一次） | 5-10 秒 |
| 页面翻译时间（缓存命中） | 1-2 秒 |
| 并发翻译数 | 2 个 |
| 缓存命中率 | 80-90%（相同内容） |

如果翻译时间明显长于基准，检查：
1. API Key 是否有效
2. 网络连接是否稳定
3. 页面元素数量（极大的页面会更慢）
4. LLM API 的响应时间

---

## 常见问题

**Q: 为什么翻译速度慢？**
A: 检查 Console 日志，如果看到网络错误，可能是 API 速度慢。如果没有错误，可能是页面元素太多。

**Q: 为什么表格格式混乱？**
A: 这已在第2阶段修复。确保使用了最新的构建版本（重新加载扩展）。

**Q: 为什么划词翻译不工作？**
A: 检查 Console，应该看到 "📥 Translation response:" 日志。如果没有，检查 API Key。

**Q: 如何查看详细日志？**
A: F12 → Console → Filter（筛选框）输入你想要的关键词（如 "API Key"、"error" 等）。

**Q: 如何导出日志用于报告？**
A:
1. F12 → Console
2. 右键 → 保存为
3. 或 Ctrl+A 全选 → Ctrl+C 复制 → 粘贴到文本编辑器

---

## 更新和重建

### 如果修改了代码
```bash
npm run build
chrome://extensions/ → 点击刷新
F12 → 关闭并重新打开 Console
```

### 如果 Console 中没有新日志
```bash
# 完全清除旧的构建
rm -rf dist/
npm run build

# 重新加载扩展
chrome://extensions/ → 禁用 → 启用
```

---

## 获取帮助

如果问题仍未解决，请收集以下信息：

```markdown
【问题描述】
翻译不工作 / 翻译失败 / 格式混乱 / 其他症状

【测试网址】
https://...

【API 配置】
- 模型：DeepSeek / Kimi / 等
- API Key 已配置：是/否
- API Key 验证结果：成功/失败/未测试

【Console 日志】
（复制关键的日志片段）

【网络环境】
- 使用代理：是/否
- 使用 VPN：是/否
- 能访问 API 网址：是/否
```

发送这些信息可以更快地诊断和解决问题。

---

*最后更新：2025-12-06*
